# Profil fuer das Deployment. Zugangsdaten werden ausschliesslich ueber Umgebungsvariablen
# (DB_URL, DB_USERNAME, DB_PASSWORD, siehe application.properties) bereitgestellt.
# Konkretes Zielsystem ist noch offen; dieses Profil dient als Grundgeruest.
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=false
